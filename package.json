{
  "name": "prj-totvs-local-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "whatsapp": "npm --prefix server/whatsapp install && npm --prefix server/whatsapp start",
    "whatsapp:mock": "npm --prefix server/whatsapp install && WHATSAPP_MOCK=true npm --prefix server/whatsapp start"
  }
}
